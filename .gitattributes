# 统一使用 LF 换行，避免 Windows / 云端构建之间的整文件 diff
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.mp4 binary

# 构建产物与锁文件不参与 diff 统计
package-lock.json -diff linguist-generated
